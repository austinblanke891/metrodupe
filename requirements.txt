# Core app
streamlit==1.36.0

# Imaging
Pillow>=10.0,<12.0

# Optional (only if you actually use PDFs elsewhere in the app)
pymupdf>=1.23,<2.0

# Widgets you mentioned
streamlit-image-coordinates>=0.1.6
streamlit-searchbox==0.1.4

# SVG → PNG renderer stack (required for the map)
cairosvg==2.7.1
cairocffi>=1.6.0
tinycss2>=1.3.0
cssselect2>=0.7.0
defusedxml>=0.7.1
